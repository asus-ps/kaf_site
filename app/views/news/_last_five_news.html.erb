
  <table width="98%" style="margin: 10px;">
    <tr>
      <td>
  <% i = 0 %>
  <% articles.each do |n| %>
  <% i = i + 1 %>
<%= color_table(i) %>
    <tr>
      <td class="NewsTdTop" colspan="2"><%=  link_to(n.title, :controller => 'news', :action => 'show',:id => n) %> </td>
    </tr>
    <tr>
      <td colspan="2"><%=  Maruku.new("#{n.body.chars.first(200)}...").to_html %> </td>
    </tr>
    <tr>
      <td class="NewsTdBotLeft"><%= link_to 'Читать полностью', :controller => 'news', :action => 'show', :id => n %> </td>
      <td class="NewsTdBot"><%=  "#{Russian::strftime(n.created_at,'%d %B %Y | %H:%M')}" %> </td>
    </tr>
</table>
  <%  end %>
        </td>
  </tr>
</table>
