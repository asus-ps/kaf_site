<h1>Список специальностей | Измененить</h1>

<% form_for([:manage,@speciality]) do |f| %>
  <%= render :partial => 'form',:locals => { :f => f, :but => 'Изменить' } %>
<% end %>

<%= link_to 'Назад', manage_specialities_path %>